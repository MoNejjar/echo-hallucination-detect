import React from 'react';

// Stub component intentionally left unused. Exists only to avoid build issues.
// The UI should not reference this component.
const InitiationPanel: React.FC = () => null;

export default InitiationPanel;